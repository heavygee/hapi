diff --git a/shared/src/sessionSummary.ts b/shared/src/sessionSummary.ts
index cef83e6e5..85f987853 100644
--- a/shared/src/sessionSummary.ts
+++ b/shared/src/sessionSummary.ts
@@ -62,7 +62,11 @@ export type SessionSummary = {
     thinking: boolean
     activeAt: number
     updatedAt: number
+    /** Optional on summary fixtures; hub Session rows always have versions. */
+    metadataVersion?: number
+    agentStateVersion?: number
     metadata: SessionSummaryMetadata | null
+    todosUpdatedAt?: number
     todoProgress: { completed: number; total: number } | null
     pendingRequestsCount: number
     pendingRequestKinds: PendingRequestKind[]
diff --git a/web/src/components/SessionList.test.ts b/web/src/components/SessionList.test.ts
index 6b4b74c04..c2827af4d 100644
--- a/web/src/components/SessionList.test.ts
+++ b/web/src/components/SessionList.test.ts
@@ -442,8 +442,7 @@ describe('expandSelectedSessionCollapseOverrides', () => {
         ])
 
         const result = expandSelectedSessionCollapseOverrides(overrides, {
-            key: 'machine-1::/work/hapi',
-            machineId: 'machine-1'
+            key: 'machine-1::/work/hapi'
         })
 
         expect(result.has('machine-1::/work/hapi')).toBe(false)
@@ -455,8 +454,7 @@ describe('expandSelectedSessionCollapseOverrides', () => {
         const overrides = new Map<string, boolean>()
 
         const result = expandSelectedSessionCollapseOverrides(overrides, {
-            key: 'machine-1::/work/hapi',
-            machineId: 'machine-1'
+            key: 'machine-1::/work/hapi'
         })
 
         expect(result.has('sessions::machine-1::/work/hapi')).toBe(false)
