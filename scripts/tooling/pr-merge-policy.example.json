{
  "schema": 1,
  "default_lane": "maintainer",
  "self_merge": {
    "github_labels": ["low-impact"],
    "allow_pr_numbers": [],
    "auto": {
      "max_changed_files": 8,
      "max_delta_lines": 120
    }
  },
  "_comment": "Copy to ~/.hapi/pr-merge-policy.json. UPSTREAM ONLY (tiann/hapi): chip stays health-only; this overlay picks lane A (maintainer/wait tiann) vs B (self-merge). Auto-B = size caps on product files only (*.test.* / *.spec.* / __tests__ excluded; pure test-only PRs stay auto-B). Product paths OK under caps. Oversized / judgment-call PRs promote via low-impact label or allow_pr_numbers. Path-kind reject (product_paths) retired 2026-08-09. Does NOT gate heavygee/hapi — fork merges are operator/Meta choice, not lane A/B."
}
