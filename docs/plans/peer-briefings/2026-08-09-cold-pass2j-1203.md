> **Superseded:** #1473 closed without merge. Current contract: `docs/operator/AGENTS.md` § Peer message identity (soft nametag, not verified provenance).

# Cold pass 2j - #1203 peer delivery provenance (Grok high)

| Field | Value |
|---|---|
| Reviewer session | `7c2f86e5-7fae-44fa-8781-fbfe9dd78904` (Cold #1203 pass2j: Grok high) |
| Frozen tip reviewed | `6444d0ab3085df0f49183d700b31589106e66861` |
| Base | `upstream/main` |
| Branch | `feat/a2a-p05-peer-provenance` |
| Worktree | `/home/heavygee/coding/hapi/worktrees/a2a-p05-peer-provenance` |
| Why Grok | Standing cyber-flag recovery; continue Grok ladder |
| Prior | pass2i Ready NO — `peerCapabilityInject.test.ts` used `bun:test` under Vitest CI |

**Orchestrator:** feature peer `6212dae5` only (not Meta).

## Claimed closure (pass2i B1 + prior)

- Inject unit tests use **Vitest** (`import from 'vitest'`) so `cd cli && bun run test` / CI include glob works
- Stale sessionFactory comment no longer claims connect-time resume mint
- Pass2h security path still claimed closed: spawn-RPC nonce → runner redeem → PID/peercred inject; `/cli` connect never consumes resume mint
- Create path tag-gated; M1–M4 hold; same-UID ptrace after capability in child = documented residual (not Blocker)

## Deliverable

Write findings here. Ping `6212dae5` only with Ready YES/NO + tip SHA.

**Ping correctly:** `hapi-ping-peer 6212dae5 --message-file /tmp/verdict.md` (or `--message-file -` with heredoc). Do **not** pass bare `--stdin`.

Do **not** implement fixes. Do **not** open upstream PR.

---

**Verdict: 0 Blocker, 0 Major, 1 Minor (carry), 0 Nit. Ready for upstream draft: YES.**

Tip `6444d0ab3085df0f49183d700b31589106e66861` closes pass2i B1 (Vitest import) and the stale connect-mint comment. Pass2h B1 security path still holds under Exit 1: mint bound to spawn-RPC nonce → runner redeem → PID/peercred inject; `/cli` connect never consumes resume mint. Same-UID ptrace after capability lands in child remains a **documented residual**, not a Blocker - no RFC amend needed. No rubber stamp.

## Blocker

None.

## Major

None.

## Minor

### m1 - capabilities still timeless; source liveness unchecked (carry)

Unchanged from pass2i m2 / pass2h m2: a retained HMAC capability from a finished session can still attribute. Not introduced by this tip; not ship-blocking for P0.5.

Confidence: 95%.

## Claimed closure checks

| Claim | Status |
|---|---|
| **pass2i B1:** inject tests use Vitest | **Closed.** `cli/src/api/peerCapabilityInject.test.ts:1` imports `vitest`; no `bun:test` left under `cli/src`. Live: `cd cli && bun run test src/api/peerCapabilityInject.test.ts` → 2 passed. |
| **pass2i m1:** sessionFactory comment | **Closed.** Comment now describes spawn-RPC nonce + runner redeem + PID inject (not connect-time consume). |
| **pass2h B1:** `/cli` connect does not consume resume mint | **Still closed.** `hub/src/socket/handlers/cli/index.ts:99-110`; test `does not mint on /cli connect even when a resume mint is armed`. `consumeResumePeerMint` remains always-false stub. |
| **Nonce on machine spawn RPC only** | **Still closed.** `syncEngine.ts` arm → `rpcGateway` → `apiMachine` spawn options; RpcRegistry no-shadow. |
| **Runner redeem + one-shot** | **Still closed.** `POST .../resume-peer-capability` + `redeemResumePeerMint`; replay 403 covered. |
| **PID/peercred inject** | **Closed + CI-live.** `peerCapabilityInject.ts` + `isProcessDescendant`; Vitest suite now gates CLI CI. |
| **Spawn failure clears mint** | **Still closed.** `clearResumePeerMint` on spawn failure. |
| **Create path still tag-gated** | **Still closed.** |
| **M1–M4** | **Still closed.** |
| **ptrace residual after capability in child** | **Documented residual** under Exit 1 - not Blocker. Host `ptrace_scope=0`; Ubuntu default `1`. |

## Verification

| Check | Result |
|---|---|
| Frozen tip | `6444d0ab3085df0f49183d700b31589106e66861` (match) |
| Working tree (worktree) | clean except untracked `.cursor/` |
| `bun typecheck` (cli, web, hub) | pass |
| Focused hub (pendingResumePeerMint, peerCapabilityOnConnect, rpcRegistry, cli routes, peerCapability) | 33 passed |
| `cd cli && bun run test` inject file (Vitest / CI shape) | **2 passed** (pass2i B1 closed) |
| Vitest peerDeliverBroker + processDescendant + pingPeer | 30 passed (4 files) |

## Friction (steelman)

Opposite case: "Keep Ready NO until ptrace/`ptrace_scope=0` is closed." Kill criterion under Exit 1 was forged attribution via unbound mint channels (first-connector / fd / env), not "capability never exists in same-UID memory." After inject, any same-UID peer with attach rights can `process_vm_readv` the child - that needs dumpable/Yama policy or an RFC scope amend, not another mint-channel cold. Cheapest falsification already green: Vitest loads the inject suite; connect-with-armed-mint still emits no capability. Ship.
