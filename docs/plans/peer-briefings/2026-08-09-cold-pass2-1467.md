# Full court press — Pass 2 (GPT Sol) — PR #1467 / #1465

Tip reviewed: `0268a3099` on `feat/a2a-p3-notify-ingest`.

**Verdict: Ready for #1462 note / public tip: NO**

M1–M4 from pass 1 verified fixed/recorded.

## Majors (must fix before undraft)

**S1. Incomplete assistant text extraction** (`hub/src/sync/workGraphNotifyIngest.ts:153-157`)
Only Codex/Claude envelopes. Valid footer in AGY `output.data.type="agy_message"` → null. Normalize all supported assistant text shapes; add engine-level coverage.

**S2. Untrusted `notify.agent` → `principal.id`** (`hub/src/sync/workGraphNotifyIngest.ts:115-136`)
Repro: `agent="forged-peer"` persists as audit principal. Bind `principal.id` to `session:${sessionId}` always; keep `notify.agent` advisory in payload/tags. (Upstream Codex already flagged related principal bind.)

**S3. Fresh `stalled` mapped to `stale`** (`hub/src/sync/workGraphNotifyIngest.ts:34-46`)
RFC staleness is via `expires_at`. Map to `blocked`/`unknown` per explicit policy; never `stale` before expiry.

**S4. Unbounded POST body** (`hub/src/web/routes/workGraph.ts:18-20,74-76`)
Bun allows ~68 MiB; 1 MiB payload stored in append-only ledger. `bodyLimit` on both POSTs + bounded schema strings/arrays.

## Verification (Sol)

- typecheck OK; hub 1029 pass; shared 229 pass
- Upstream Codex review passed; Test red on unrelated `cli/src/claude/claudeRemote.test.ts` timeout (local targeted 6/6 OK)
