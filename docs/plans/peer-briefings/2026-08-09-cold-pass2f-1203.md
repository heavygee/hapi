# Cold pass 2f - #1203 peer delivery provenance (Grok high)

| Field | Value |
|---|---|
| Reviewer session | *(filled by cold)* |
| Frozen tip | `ac731000d1e4214f9243821132360e99d704c062` |
| Base | `upstream/main` |
| Branch | `feat/a2a-p05-peer-provenance` |
| Worktree | `/home/heavygee/coding/hapi/worktrees/a2a-p05-peer-provenance` |
| Why Grok | OpenAI Sol pass2e cyber-flagged; standing recovery → `cursor-grok-4.5-high` |
| Prior | pass2e-alt Claude Ready NO → feature peer landed B1/M1–M4 on this tip |

**Orchestrator:** feature peer `/sessions/6212dae5-8a60-4284-b7a5-c09aa3571ce4` (not Meta). Ping **only** that session when done.

## Your job

Defensive authz / provenance-correctness cold read of `upstream/main...HEAD` at the frozen tip. Not a rubber stamp. Residual Blocker/Major + regressions from the pass2e-alt fix commit.

Do **not** implement fixes. Do **not** open/undraft upstream PR.

### Shared commands

```bash
cd /home/heavygee/coding/hapi/worktrees/a2a-p05-peer-provenance
git fetch upstream
git rev-parse HEAD   # must be ac731000d1e4214f9243821132360e99d704c062
git log --oneline upstream/main..HEAD
git diff upstream/main...HEAD
bun typecheck
# focused packages touched + note unrelated flakes
```

- Rubric: `docs/tooling/cold-pr-review-rubric.md` + `.github/prompts/codex-pr-review.md`
- Contract: `~/coding/hapi/docs/plans/2026-08-03-a2a-control-plane-rfc.md` § Revision 2026-08-09
- Issue: https://github.com/tiann/hapi/issues/1203
- Prior findings: `2026-08-09-cold-pass2e-alt-1203.md`

### Product focus

1. Does hub-derived sender attribution hold? Any client-claimable source id?
2. Resume mint-proof must not remain readable by same-UID siblings via process environ
3. Wrapped `hapi ping-peer` must not permanently break on Darwin
4. Terminal resume must not permanently brick peer messaging (unattributed OK if documented)
5. Broker client must not trust a rebinding same-UID listener
6. Spawn RPC must not let another same-namespace socket steal the mint gate
7. No ledger/events/handoff/P2 scope creep

### Claimed closures to re-verify (pass2e-alt fix tip)

- Tag via stdio fd 3 + `HAPI_PEER_SESSION_TAG_FD` (fd number only), not tag-in-env
- Darwin peercred + `ps` ppid walk
- Terminal/broker-less → unattributed fallback (not permanent auth_failed)
- Client ancestor check on broker connect; random socket path suffix
- `RpcRegistry` refuses method overwrite while another socket owns it

### Deliverable

1. Write full findings to **this file** (overwrite placeholder header fields)
2. Ping feature peer `6212dae5` **only** with: verdict counts, Ready-for-upstream-draft yes/no, tip SHA, pointer to this file + your session
3. Message must open with `From: /sessions/<your-id>`
4. Idle after ping (orchestrator archives)

Severity: Blocker / Major / Minor / Nit. Evidence `path:line`. No praise. Confidence ≥80%.
Public voice: A2A / Layer 0 only — no estate product codenames in findings that might be pasted to GitHub.
