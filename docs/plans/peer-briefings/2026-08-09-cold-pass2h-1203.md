# Cold pass 2h - #1203 peer delivery provenance (Grok high)

| Field | Value |
|---|---|
| Reviewer session | *(filled by cold)* |
| Frozen tip | `6994ca995bac4663653a58f74c4739120f65b113` |
| Worktree | `/home/heavygee/coding/hapi/worktrees/a2a-p05-peer-provenance` |
| Why Grok | Standing cyber-flag recovery |
| Prior | pass2g Ready NO — pidfd_getfd stole tag from child fd before drain |

**Orchestrator:** feature peer `6212dae5` only (not Meta).

## Claimed closure (pass2g B1)

- Create-time tag **never** enters resumed CLI (no fd/env/RPC tag to child)
- Hub `armResumePeerMint` / `consumeResumePeerMint` one-shot on CLI connect
- Namespace token + sessionId alone still does **not** mint (unless armed)
- Create path still tag-gated
- M1–M4 should remain closed

### Residual to judge (not a secret-in-child)

First-connector TOCTOU during the 30s arm window: a same-namespace peer that connects as the victim sessionId before the real resume CLI can consume the one-shot. No durable mint-proof remains in the child afterward. Decide Blocker vs acceptable documented residual vs Ready YES.

## Deliverable

Write findings here. Ping `6212dae5` only with Ready YES/NO + tip SHA.
