> **Superseded:** #1473 closed without merge. Current contract: `docs/operator/AGENTS.md` § Peer message identity (soft nametag, not verified provenance).

# Cold pass 2i - #1203 peer delivery provenance (Grok high)

| Field | Value |
|---|---|
| Reviewer session | `6a2159d9-ed38-4122-9a98-eeb5ed442d51` (Cold #1203 pass2i: Grok high) |
| Frozen tip reviewed | `5db7695d57607f958dd25ca68065640b2befa638` |
| Base | `upstream/main` |
| Branch | `feat/a2a-p05-peer-provenance` |
| Worktree | `/home/heavygee/coding/hapi/worktrees/a2a-p05-peer-provenance` |
| Why Grok | Standing cyber-flag recovery; continue Grok ladder |
| Prior | pass2h Ready NO — first-connector TOCTOU on hub one-shot mint |

**Orchestrator:** feature peer `6212dae5` only (not Meta).

## Claimed closure (pass2h B1)

- Hub arms a **spawn-RPC nonce** (`armResumePeerMint` → `resumePeerMintNonce` on machine spawn only)
- `/cli` connect **never** consumes a resume mint (tag-only create path remains)
- Runner redeems `POST /cli/sessions/:id/resume-peer-capability` with the nonce → capability
- Runner injects capability via PID/peercred-checked unix socket (`peerCapabilityInject.ts`)
- Child pulls via `receivePeerCapabilityFromRunner` + `applyPeerSessionCapability`
- Spawn failure clears the armed mint (`clearResumePeerMint`)
- Create path still tag-gated; M1–M4 should remain closed

### Residual to judge

- Short inject race / same-UID ptrace of child memory after capability lands (`ptrace_scope=0`)
- Nonce bearer on machine RPC + runner process (RpcRegistry no-shadow holds)
- Decide Blocker vs documented residual vs Ready YES under Exit 1 kill criterion

## Deliverable

Write findings here. Ping `6212dae5` only with Ready YES/NO + tip SHA.

Do **not** implement fixes. Do **not** open upstream PR.

---

**Verdict: 1 Blocker, 0 Major, 2 Minor, 0 Nit. Ready for upstream draft: NO.**

Pass2h B1 (first-connector TOCTOU) is **closed** on this tip under Exit 1: mint is bound to the machine spawn-RPC nonce + runner redeem + peercred/PID inject, not anonymous `/cli` connect. Residual same-UID ptrace after the capability is in child (or briefly runner) memory is a **documented residual**, not a Blocker - same class prior colds already parked once the secret is no longer on an inheritable fd/env. Ship is still blocked because the new CLI inject tests are written for `bun:test` while CLI CI runs Vitest, so `bun run test` / `.github/workflows/test.yml` fails on import. No rubber stamp.

## Blocker

### B1 - `peerCapabilityInject.test.ts` uses `bun:test`; CLI package CI is Vitest

**Evidence:** `cli/src/api/peerCapabilityInject.test.ts:1`; `cli/package.json` `"test": "... && vitest run"`; `cli/vitest.config.ts:8` `include: ['src/**/*.test.ts']`; `.github/workflows/test.yml` runs `bun run test`.

**Falsification (live):**

```text
cd cli && bun run test src/api/peerCapabilityInject.test.ts
→ Error: Cannot find package 'bun:test'
```

Hub correctly uses `bun test`; CLI peers (`peerDeliverBroker.test.ts`, `processDescendant.test.ts`) correctly use Vitest. This tip is the only CLI file importing `bun:test`. Vitest will load it via the include glob and fail the CLI suite before any inject assertions run. The security tests therefore do not gate CI.

**Suggested fix:** rewrite the file to `vitest` imports (match `peerDeliverBroker.test.ts`), or move it under hub only if that is wrong ownership. Re-run `cd cli && bun run test src/api/peerCapabilityInject.test.ts`.

Confidence: 100%.

## Major

None new. Pass2h B1 security claim holds (see Claimed closure checks).

## Minor

### m1 - stale comment still describes connect-time resume mint

`cli/src/agent/sessionFactory.ts:326-329` still says the hub one-shot is "consumed on CLI socket connect". Tip `5db7695d5` removed that path (`consumeResumePeerMint` always false; connect is tag-only). Misleading for the next cold / upstream reviewer.

Confidence: 100%.

### m2 - capabilities still timeless; source liveness unchecked (carry)

Unchanged from pass2h m2: retained HMAC capability from a finished session can still attribute. Not introduced by this tip.

Confidence: 95%.

## Claimed closure checks

| Claim | Status |
|---|---|
| **pass2h B1:** `/cli` connect does not consume resume mint | **Closed.** `hub/src/socket/handlers/cli/index.ts:99-110`; test `does not mint on /cli connect even when a resume mint is armed`. |
| **Nonce on machine spawn RPC only** | **Closed.** `syncEngine.ts:2801-2822` → `rpcGateway` → `apiMachine` spawn options; `RpcRegistry` still refuses method shadow. |
| **Runner redeem + one-shot** | **Closed.** `POST .../resume-peer-capability` + `redeemResumePeerMint`; replay 403 covered. |
| **PID/peercred inject** | **Closed in code.** `peerCapabilityInject.ts` + `isProcessDescendant`; socket path random + `0600`. Unit coverage exists but is CI-dead (B1). |
| **Spawn failure clears mint** | **Closed.** `clearResumePeerMint` on `spawnResult.type !== 'success'` (fixes prior pass2h m1 for this path). |
| **Create path still tag-gated** | **Closed.** |
| **M1–M4** | **Still closed** (broker / unattributed / sock / RpcRegistry). |
| **ptrace residual after capability in child** | **Documented residual** under Exit 1 - not Blocker. Host under review has `ptrace_scope=0`; Ubuntu default remains `1`. |

## Verification

| Check | Result |
|---|---|
| Frozen tip | `5db7695d57607f958dd25ca68065640b2befa638` (match) |
| Working tree (worktree) | clean except untracked `.cursor/` |
| `bun typecheck` (cli, web, hub) | pass |
| Focused hub (pendingResumePeerMint, peerCapabilityOnConnect, rpcRegistry, cli routes) | 31 passed |
| `bun test` inject file (bun runner) | 2 passed |
| `cd cli && bun run test` inject file (Vitest / CI shape) | **FAIL** - B1 |
| Vitest peerDeliverBroker + processDescendant | 5 passed |
| Vitest pingPeer.test.ts | 23 passed |

## Friction (steelman)

Opposite case: "Security of pass2h B1 is done; demote the test import to Major and Ready YES." Kill criterion for Ready is upstream-draft readiness. Rubric Blocker includes broken CI. Cheapest falsification already failed: Vitest cannot import the new suite. Fix is a one-line framework swap - do that, re-freeze, then Ready YES is plausible without another forge ladder if no new mint path appears.

Same-UID ptrace after inject: still possible on `ptrace_scope=0`. That was explicitly in-scope to judge. Binding the mint to the authenticated machine/runner channel satisfies Exit 1; requiring a non-ptraceable capability forever would need an RFC amend (shared-UID mutual trust or dumpable/Yama policy). Do not reopen B1 on that residual alone.
