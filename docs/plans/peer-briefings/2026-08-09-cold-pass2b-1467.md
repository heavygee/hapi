# Full court press — Pass 2b Sol recheck — PR #1467 / #1465

Frozen tip: `e2f678527047cdf1d06abde52cceb24d1ded631a`

**Ready to undraft / #1462 note: NO**

## Verified fixed
- S1 AGY / extractAssistantPlainText + engine coverage
- S2 principal.id = `session:${sessionId}`; notify.agent advisory only
- S3 stalled→blocked; self-stale→unknown

## Residual Major (S4 incomplete)

Production notify elevation builds `WorkGraphEventCreate` but **inserts without `WorkGraphEventCreateSchema` validation**.

Repro: persisted agent message + SyncEngine inserted a **2,049-character summary** although the schema rejects summaries over 2,048. Untrusted footer fields can bypass the new ledger bounds.

**Fix:** validate at store or ingest boundary; add regression test.

## Nonblocking
- Hono 4.11.2: unknown-length overflow → 400 not 413 (repro wrote zero rows)
- Prior limit=1.5 → 500 Minor remains

## Verification (Sol)
typecheck green; hub 1031 pass / 3 skip; shared 233 pass; focused 60 pass.
