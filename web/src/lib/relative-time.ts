/**
 * Kebab-case alias of `@/lib/relativeTime` for callers that import that path.
 * Canonical implementation lives in `relativeTime.ts`.
 */
export {
    formatAbsoluteDateTime,
    formatRelativeTime,
    type TFunc
} from './relativeTime'
